# HTTP client used by the scraper to fetch listing pages.
requests>=2.31.0

# HTML parser used to extract product data from fetched pages.
beautifulsoup4>=4.12.0

# PostgreSQL driver used for DB connections and writes.
psycopg[binary]>=3.2.0

# Loads environment variables from .env files for local/dev configuration.
python-dotenv>=1.0.0

# Test runner used by the test suite under /tests.
pytest>=8.0.0
